import numpy as np
from solvers.plotters import plot_integrator_results


def main():
    # todo: solve_ivp in a plotting script like assn 1
    # todo: main calls plot which calls integrator
    # plot_integrator_results(title,
    #                             args=(initial_temperatures, coefficients),
    #                             volcano_model=None, solvers=['LSODA'])
    return


if __name__ == "__main__":
    main()
